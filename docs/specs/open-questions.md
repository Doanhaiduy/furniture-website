# Open Questions And Assumptions

These items are now scoped so coding can start on foundation and homepage work. Items marked `Slice blocker` must be answered before implementing the affected slice, not before all coding.

* [ ] IDRelated RequirementszQuestion Or AssumptionStatusImpactOQ-001FR-03, FR-04Launch taxonomy assumes two fixed top-level groups plus editable child categories. Final child category list, attribute labels, and price bands still need business input.Slice blocker for full catalog filtersProduct model supports this without architecture changes.OQ-002FR-05Search baseline: case-insensitive, partial keyword matching across localized product name, category, brand/series, and summary; accent-insensitive Vietnamese search is preferred for launch.Slice blocker for search tuningDetermines indexes and relevance tests.OQ-003FR-07-PUB, FR-07-ADMQuote form baseline requires name, phone, message, source path, locale, and optional email/product/category/company. Final recipient emails and lead retention policy need approval.Slice blocker for quote flowAffects Zod schema, Resend config, privacy policy copy, and admin filters.OQ-004FR-08-PUB, FR-08-ADMGoogle Maps Embed is selected. Launch showroom addresses, embed URLs, fallback URLs, opening hours, and hotline ownership need content approval.Slice blocker for showroom contentDoes not change architecture.OQ-005FR-09Social baseline supports configured official links and share URLs. Final platforms expected: Facebook, Zalo, YouTube, TikTok if provided.Slice blocker for social UISite settings model supports variable platforms.OQ-006FR-10, NFR-05Role model is resolved as Option A: Admin and Editor only. Need first Admin account bootstrap process.Open operational detailRequired before CMS goes live.OQ-007FR-12-PUB, FR-12-ADMLocalized content strategy is resolved: Payload localized fields/objects per `vi` and `en`, with localized slugs for public detail pages.ResolvedImplement as documented.OQ-008FR-11AI baseline uses OpenAI for draft-only product/blog/homepage/SEO/translation assistance. Need final model name, spend limits, and moderation/escalation policy before implementation.Slice blocker for AIAI can be deferred without blocking core site.OQ-009NFR-01Performance baseline targets Vercel production or preview with production-like data and throttled Lighthouse Mobile. Final PageSpeed test URL and data volume need approval.Slice blocker for performance signoffEarly slices can use local timing plus build checks.OQ-010NFR-02Monitoring tool is not selected. Baseline requires uptime checks for public site and Payload health endpoint plus alert owner.Release blockerDoes not block coding.OQ-011NFR-04Browser versions are not pinned. Baseline supports current stable Chrome, Edge, Firefox, Safari, and Coc Coc at release time.Release blockerBrowser MCP is the primary browser compatibility smoke path. Playwright backup covers Chromium/Firefox/WebKit only when CI/headless matrix evidence is required; Coc Coc may require manual smoke.OQ-012NFR-06SEO checklist baseline is defined in `docs/architecture/content-workflow.md` and `docs/specs/test-plan.md`; final brand keywords and default OG image need content approval.Slice blocker for final SEO copyDoes not change metadata architecture.OQ-013NFR-05Media baseline allows JPEG, PNG, WebP, AVIF, and MP4/WebM only where a video field exists; max image size 10 MB, max video size 100 MB unless Cloudinary plan limits require lower.Open operational detailAffects Cloudinary presets and validation.OQ-014FR-01, FR-02, FR-12-ADMFinal Vietnamese and English homepage/about copy and launch images are not supplied. Seed/demo content must be clearly marked if used before final content.Content blocker for launchDoes not block CMS model coding.
